<<<<<<< HEAD
----------COMO CONFIGURAR O BANCO DE DADOS----------

1. Instale o XAMPP.

2. Coloque a pasta do projeto dentro de `C:\xampp\htdocs`.

3. Inicie `Apache` e `MySQL` no painel do XAMPP.

4. Crie o banco `imperio_do_choco` no phpMyAdmin.

5. Copie `.env.example` para `.env` e ajuste as credenciais do banco.
   Recomendacao senior: use um usuario dedicado da aplicacao, nao `root`.

6. Rode as migracoes:
   `C:\xampp\php\php.exe database\migrate.php`

7. Se quiser criar o administrador inicial, execute o arquivo `criar_admin.sql`.

8. Teste a conexao:
   `http://localhost/imperio-do-choco/teste_banco.php`

9. Login administrativo:
   Email: `admin@imperiodochocolate.com`
   Senha: `admin123`

10. Fluxo principal:
   `http://localhost/imperio-do-choco/login.php`
   depois
   `http://localhost/imperio-do-choco/index.php`

----------OBSERVACOES IMPORTANTES----------

- As tabelas nao sao mais criadas automaticamente pelas rotas PHP.
- Se o schema nao existir, a aplicacao vai pedir para executar `database/migrate.php`.
- O carrinho agora salva `produto_id` quando houver produto do banco e ainda preserva snapshot de nome, preco e imagem.
- O cadastro de usuarios agora depende da chave unica do banco para evitar corrida de concorrencia.
- O banco agora inclui `enderecos`, `pedidos`, `pedido_itens` e `estoque_movimentacoes`.
- Produtos removidos no painel agora usam soft delete: saem da vitrine sem perder historico.
=======
----------Testar Banco de Dados----------

1. Baixe e instale o XAMPP na sua máquina

2. Pegue a versão do commit do GitHub que está com banco de dados e coloque DENTRO da pasta "htdocs" do XAMPP, geralmente é esse caminho: "C:\xampp\htdocs", cole a pasta do projeto DENTRO dela

3. Após colar a pasta, abra o painel "XAMPP Control Panel" que você acabou de instalar, e clique em "Start" nas opções ->> "MySQL" e "Apache"(As duas opções tem que ficar VERDES)

4. Após as duas opções ficarem VERDES vá no seu navegador(Chrome ou Edge) e aonde você digita os links, coloque os seguintes links(NA ORDEM):

- Abra o link "http://localhost/imperio-do-choco/login.php"(Copie somente dentro das ASPAS)
- faça o login com os seguintes dados: Email: admin@imperiodochocolate.com
                                       Senha: admin123
- Após o login, o vai abrir um novo site (admin.php) que no caso é o painel admin, aqui é onde os administradores(Vendedores) irão cadastrar os próprios chocolates, cadastre um só para testes
- Após cadastrar o chocolate teste, abra uma nova aba e abra o seguinte link: "http://localhost/imperio-do-choco/index.php"(Copie somente dentro das ASPAS) e pronto, caso queira testar novamente, só ir adicionando mais
chocolates.
- Caso queira testar SOMENTE o banco de dados, sem muita burocracia, você pode testar o seguinte link: "http://localhost/imperio-do-choco/teste_banco.php"(Copie somente dentro das ASPAS)


Por enquanto é isso, até.(CASO QUEIRA FAZER ALTERAÇÕES NO BANCO, ALTERAÇÕES BOAS, COLOQUE AS ALTERAÇÕES AQUI EM BAIXO!!!!!!!!!!!!!!!
>>>>>>> 42de13b18067624c8c82cf4681fed6951fc785dd
